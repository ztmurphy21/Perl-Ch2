#!/usr/bin/perl

$numOne = 100;
$numTwo = 500;
$numThree = 250;

if ($numOne gt $numThree){
	print "$numOne is greater than $numThree.\n";
	} 
	else {
	print "$numThree is greater than $numOne.\n";
	}
	
if ($numTwo gt $numThree){
	print "$numTwo is greater than $numThree.\n";
	} 
	else {
	print "$numThree is greater than $numTwo.\n";
	}

	
	